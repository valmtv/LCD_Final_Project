let t1 = (10, 20, 30) in
printInt(t1.1);
printInt(t1.2);
printInt(t1.3);
printEndLine();

(let t2 = (1, true, 3) in
  printInt(t2.1);
  printBool(t2.2);
  printInt(t2.3);
  printEndLine();

  (let t3 = (100, (200, 300)) in
    printInt(t3.1);
    printInt(t3.2.1);
    printInt(t3.2.2);
    printEndLine();

    (let swap = fun (p: (int * int)) -> (p.2, p.1) in
      let t4 = (5, 10) in
      let swapped = swap(t4) in
      printInt(swapped.1);
      printInt(swapped.2);
      printEndLine();

      (let r = new(50) in
        let t5 = (r, 60) in
        printInt(!(t5.1));
        t5.1 := 51;
        printInt(!(t5.1))
      )
    )
  )
)