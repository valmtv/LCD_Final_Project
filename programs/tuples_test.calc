let t1 = (10, 20, 30) in
printInt(t1.0);
printInt(t1.1);
printInt(t1.2);
printEndLine();

(let t2 = (1, true, 3) in
  printInt(t2.0);
  printBool(t2.1);
  printInt(t2.2);
  printEndLine();

  (let t3 = (100, (200, 300)) in
    printInt(t3.0);
    printInt(t3.1.0);
    printInt(t3.1.1);
    printEndLine();

    (let swap = fun (p: (int * int)) -> (p.1, p.0) in
      let t4 = (5, 10) in
      let swapped = swap(t4) in
      printInt(swapped.0);
      printInt(swapped.1);
      printEndLine();

      (let r = new(50) in
        let t5 = (r, 60) in
        printInt(!(t5.0));
        t5.0 := 51;
        printInt(!(t5.0))
      )
    )
  )
)